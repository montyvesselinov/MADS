execute_process(
  COMMAND make install
  WORKING_DIRECTORY "@SOURCE_DIR@"
  RESULT_VARIABLE result
)

if (result)
  set(msg "Install command failed in @SOURCE_DIR@")
  foreach(arg IN LISTS command )
    set(msg "${msg} '${arg}'")
  endforeach()
  message(FATAL_ERROR ${msg})
endif()  
